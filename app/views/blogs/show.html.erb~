<div class="container">
  <h1><%= @blog.name %></h1>
  <p class="lead">Description:<%= @blog.description %></p>
  <p id="notice"><%= notice %></p>
  <% if @blogposts.empty? %>
    This user has no posts.
  <% end %>
  <div class="blog-posts">
    <%= render :partial => "posts/show_post_row", :collection => @blogposts, :as => :post %>
  </div>
</div>
